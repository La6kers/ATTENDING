# ============================================================
# ATTENDING AI — Frontend CI/CD
# .github/workflows/frontend.yml
#
# NOTE: This file was DISABLED because it incorrectly used pnpm.
# The ATTENDING monorepo uses npm workspaces (per locked decisions).
# The canonical frontend CI is .github/workflows/ci.yml.
# The canonical deploy pipeline is .github/workflows/deploy.yml.
# ============================================================

name: "[DISABLED] Old Frontend CI — use ci.yml + deploy.yml"

on:
  workflow_dispatch: # Manual only — effectively disabled

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Use ci.yml (CI) and deploy.yml (CD) instead."
