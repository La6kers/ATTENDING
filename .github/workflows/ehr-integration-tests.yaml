# DISABLED — FHIR sandbox credentials not yet configured.
# Re-enable when:
#   1. EPIC_CLIENT_ID is set in GitHub Environment secrets
#   2. Epic App Orchard sandbox registration is approved
#   3. At least one FHIR resource mapper test is written
#      in apps/shared/lib/fhir/__tests__/
# See docs/STAGING_SETUP.md Step 5 for Epic B2C setup.
name: "[DISABLED] EHR Integration Tests — re-enable when FHIR sandbox is ready"
on:
  workflow_dispatch:
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "FHIR integration tests are not yet active. See docs/STAGING_SETUP.md."
