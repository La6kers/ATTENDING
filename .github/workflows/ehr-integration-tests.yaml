# DISABLED: FHIR integration is not yet connected to a live sandbox.
# This workflow references packages/fhir which does not have active test scripts.
# Re-enable when Epic/Oracle sandbox credentials are configured and
# FHIR client integration testing is ready to run in CI.
name: "[DISABLED] EHR Integration Tests — re-enable when FHIR sandbox is ready"

on:
  workflow_dispatch: # Manual only — effectively disabled

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "FHIR integration tests are not yet active. See docs/CURRENT_STATE.md for status."
