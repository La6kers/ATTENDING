# ============================================================
# ATTENDING AI — Canonical CI Pipeline
# .github/workflows/ci.yaml
#
# IMPORTANT: This file replaces the old ci.yaml which incorrectly
# referenced PostgreSQL and pnpm. The ATTENDING platform uses
# SQL Server and npm (per locked architectural decisions).
#
# This workflow was DISABLED because ci.yml (with .yml extension)
# is the canonical frontend CI. See ci.yml for the active pipeline.
#
# Do not re-enable this file.
# ============================================================

# This file is intentionally disabled to prevent confusion with ci.yml
# To prevent GitHub Actions from running this, the 'on' trigger is removed.

name: "[DISABLED] Old CI — use ci.yml instead"

on:
  workflow_dispatch: # Manual only — effectively disabled

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Use ci.yml instead."
