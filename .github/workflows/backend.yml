# DISABLED — superseded by backend-ci.yml (CI) and deploy.yml (CD).
# This file is kept only so git history is preserved.
# Do not re-enable. Delete this file when cleaning up the repo.
name: "[DISABLED] backend.yml — use backend-ci.yml and deploy.yml"
on:
  workflow_dispatch:
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. See backend-ci.yml and deploy.yml."
