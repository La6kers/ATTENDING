# DISABLED: Superseded by backend-ci.yml (CI) and deploy.yml (CD).
# backend-ci.yml handles: build, test, security scan, Docker image build + health check.
# deploy.yml handles: staging and production deployments for all services.
name: "[DISABLED] Old Backend CI/CD — use backend-ci.yml + deploy.yml"

on:
  workflow_dispatch: # Manual only — effectively disabled

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Use backend-ci.yml (CI) and deploy.yml (CD) instead."
